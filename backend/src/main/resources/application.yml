spring:
  application:
    name: credit-scoring
  datasource:
    url: jdbc:postgresql://localhost:5432/credit_scoring
    username: postgres
    password: Purvesh@123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp-relay.brevo.com
    port: 587
    username: purveshmali99@gmail.com
    password: ${SMTP_PASSWORD:your-smtp-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

security:
  jwt:
    secret-key: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
    expiration: 900000 # 15 minutes
    refresh-token:
      expiration: 604800000 # 7 days

logging:
  level:
    org.springframework.security: DEBUG
    com.fintech.credit: DEBUG

gemini:
  api:
    key: ${GEMINI_API_KEY:your-gemini-api-key}

ml:
  api:
    url: http://127.0.0.1:8000/api/score
